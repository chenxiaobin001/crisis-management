<%= form_for(@group_doc) do |f| %>
  <% if @group_doc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_doc.errors.count, "error") %> prohibited this group_doc from being saved:</h2>

      <ul>
      <% @group_doc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.number_field :group_id %>
  </div>
  <div class="field">
    <%= f.label :document_id %><br>
    <%= f.number_field :document_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
