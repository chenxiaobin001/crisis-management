<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pennkey %><br>
    <%= f.text_field :pennkey %>
    <%= show_field_error(@user, :pennkey) %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
    <%= show_field_error(@user, :email) %>
  </div>
  <div class="field">
    <%= f.label :school %><br>
    <%= f.text_field :school %>
    <%= show_field_error(@user, :school) %>
  </div>
  <div class="field">
    <%= f.label :privilege %><br>
    <%= f.number_field :privilege %>
    <%= show_field_error(@user, :privilege) %>
  </div>
  <div class="actions">
    <%= f.submit %>
    <%= f.submit 'Clear Form', :type => 'reset' %>
  </div>
<% end %>
